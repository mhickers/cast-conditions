# No API keys needed for Open-Meteo or NOAA — they are fully free!
# The Anthropic API key is handled automatically when deployed via Vercel
# with the Claude integration, or add it here for local dev:
# REACT_APP_ANTHROPIC_API_KEY=sk-ant-...
